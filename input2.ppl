
data List a = Nil | Cons a (List a );

define not = \ a. if a then False else True;

define even = \ls. case ls of
	| Nil -> True
    --| Cons x Nil -> False
    | Cons x y -> not (even y);

even (Cons Nil)